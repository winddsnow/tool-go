server:
  address: ":8000"

database:
  default:
    link: "pgsql:prod_user:prod_password@tcp(127.0.0.1:5432)/tool_go_prod?sslmode=disable"
    debug: false

logger:
  level: "warning"
  stdout: false

jwt:
  secret: "prod-jwt-secret-key-change-me"
  expires: "24h"
  issuer: "tool-go"
